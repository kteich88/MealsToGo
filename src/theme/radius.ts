import { Radius } from "./types";

/**
 * The corner radius for rounding element edges
 * sm: small; 8px; buttons, small content
 * md: medium; 12px; photos, cards, medium content
 * lg: large; 26px; large UI elements
 */
export const radius: Radius = {
  sm: 8,
  md: 12,
  lg: 26,
  giant: 180,
};
